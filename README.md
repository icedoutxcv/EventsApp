# Description
EventsApp is app that keeps track of days until or since important events. 

# Details
* for iOS
* built using Swift without 3rd party libraries
* MVVM architecture using Coordinator pattern
* CoreData as database (only offline functionality)
